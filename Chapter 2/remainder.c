#include<stdio.h>

int main(){
    int a, b;
    a = 5, b=2;
    // % is used for reminder
    // the sign of reminder is same as the sign of numerator
    printf("5 when divided by 2 leaves a reminder of %d", 5%2);
    return 0;
}