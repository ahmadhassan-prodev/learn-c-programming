#include<stdio.h>
#include<string.h>

int main(){
    char str[]= "Ahamd Hassan";
    int a = strlen(str);
    // strlen is used to get the length of string
    printf("The length of string is %d\n",a);
    
    return 0;
}