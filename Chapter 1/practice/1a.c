// write a program to calculate the area of rectangle
#include <stdio.h>
int main(){
    int length = 4;
    int width = 6;
    int Area;
    Area = length * width;
    printf("The are of rectangle is %d", Area);
    return 0;

}